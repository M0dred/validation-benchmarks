services:
  web:
    build:
      context: ./app
      args:
      - FLAG
    ports:
    - 80
    healthcheck:
      test:
      - CMD
      - curl
      - -f
      - http://127.0.0.1:80/
      timeout: 5s
      retries: 30
      interval: 5s
